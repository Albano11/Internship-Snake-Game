import React from 'react';
import GameBoard from './GameBoard';

const App: React.FC = () => {
  return (
    <div>
      <h1>Snake Game</h1>
      <GameBoard />
    </div>
  );
};

export default App;
